// Dpendencias principales

**CREAR .env ya que git no deja subirlo porque es un archivo privado el formato de este esta abajo en IMPORTANTE**


Solo ejecutar npm install pero si es necesario estas son las dependencias

Para correr en el terminal backend npm run dev (Si recibe el mensaje de que se escucha ya esta levantado)

//npm install express pg cors dotenv
//npm install --save-dev typescript ts-node @types/node @types/express nodemon
// npm i --save-dev @types/cors

estructura

src/
  index.ts          # arranca el servidor
  .env              # variables de entorno importantes antes de montar el backend hay que crearla
  app.ts            # configuración de Express
  config/
    env.ts          # variables de entorno
  db/
    pool.ts         # conexión con PostgreSQL
  routes/
    index.ts        # rutas principales
    obra.routes.ts  # rutas de la entidad Obra
  controllers/
    obra.controller.ts  #controlador de consultas, insert, delete y updates
  models/
    obra.model.ts       #variables de obras
  middlewares/
    errorHandler.ts    # manejo de errores

*****IMPORTANTE***

    .env:

    PORT=5000
CORS_ORIGINS=http://localhost:5173,http://localhost:5174
DB_HOST=localhost
DB_PORT=5432
DB_NAME=museo_bd (nombre de tu base de datos)
DB_USER=postgres
DB_PASSWORD=123456 (tu contraseña postgres)
JWT_SECRET=1234 (contraseña ultrasegura esto hay que colocar una contraseña fuerte mientras usamos esta)


    esto para generar token e encriptamiento 

    npm install jsonwebtoken bcryptjs
    npm install --save-dev @types/jsonwebtoken @types/bcryptjs

    npm install bcrypt
npm install --save-dev @types/bcrypt